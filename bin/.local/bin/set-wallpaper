#!/usr/bin/env bash
# Set IMAGE as the wallpaper.
set -euo pipefail

img="${1:?usage: set-wallpaper IMAGE}"
dest="$HOME/.local/share/wallpaper.jpg"

[[ "$img" -ef "$dest" ]] || cp -f -- "$img" "$dest"

qs -c shell ipc call wallpaper reload &>/dev/null || true
