#!/usr/bin/env bash
# Set IMAGE as the swaybg wallpaper.
set -euo pipefail

img="${1:?usage: set-wallpaper IMAGE}"
dest="$HOME/.local/share/wallpaper.jpg"

[[ "$img" -ef "$dest" ]] || cp -f -- "$img" "$dest"

pkill -x swaybg 2>/dev/null || true
swaybg -i "$dest" -m fill >/dev/null 2>&1 &
disown
