# Monitor configuration — single source of truth.
# Change $external here when switching monitors; everything else follows.

$laptop   = eDP-1
$external = desc:LG

monitor = $external, preferred, 0x0, 1.875
monitor = $laptop,   1920x1200@60, auto-right, 1.25
